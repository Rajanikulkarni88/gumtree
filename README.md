This repository is for tests from Gumtree.
Steps to install and execute tests via command line:

* Install latest node from site
* go to project folder
* npm install cypress --save-dev
* npx cypress run 
